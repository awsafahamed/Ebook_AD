@{
    var orderPlacedMessage = TempData["OrderPlacedMessage"] as string;
}

@if (!string.IsNullOrEmpty(orderPlacedMessage))
{
    <div class="alert alert-success">
        @orderPlacedMessage
    </div>
}

<h2>Your Order Details</h2>
<p>Total: @ViewBag.OrderTotal</p>
<!-- Display more order details here as needed -->
